# The end-to-end suite runs against the browser-local demo backend, so it needs
# no Supabase project and never touches real data. Blank on purpose: Vite loads
# this instead of .env.development.local when started with `--mode e2e`.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
